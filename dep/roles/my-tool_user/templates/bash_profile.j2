if [ -f ~/.bashrc ]; then
	source ~/.bashrc
fi

PATH="$HOME{{ eb_install_dir }}/bin:$PATH"
PATH="$HOME/lmod/lmod/libexec:$PATH"
export PATH

export PYTHONPATH="$HOME{{ eb_install_dir }}/lib/python{{ py_version }}/site-packages"
export EB_PYTHON="python{{ py_version }}"
export EASYBUILD_MODULES_TOOL="Lmod"
export EASYBUILD_PREFIX="$HOME{{ eb_install_dir }}"
